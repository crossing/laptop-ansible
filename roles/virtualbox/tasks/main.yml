- include: "add-repo-{{ ansible_distribution }}.yml"

- name: install virtualbox
  package: name={{ item }} state=present
  with_items:
    - VirtualBox-{{ version }}
    - dkms

- name: add user to vbox and disk groups
  user:
    name: "{{ item[0] }}"
    group: "{{ item[1] }}"
    append: yes
  with_nested:
    - "{{ users }}"
    - ['vboxusers', 'disk']
