- name: install packages
  package: name={{ item }} state=present
  with_items:
    - zsh
    - emacs
    - git
    - stow
    - fasd
    - hplip-gui
    - "{{ jre8_package }}"
    - "{{ jdk8_package }}"
    - icedtea-web
    - texlive-scheme-full
    # develop libs
    - openssl-devel
    - readline-devel
    - zlib-devel

- include: "{{ ansible_distribution }}/packages.yml"
